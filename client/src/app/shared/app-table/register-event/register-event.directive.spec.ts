import { RegisterEventDirective } from "./register-event.directive";

describe("RegisterEventDirective", () => {
  it("should create an instance", () => {
    const directive = new RegisterEventDirective();
    expect(directive).toBeTruthy();
  });
});
